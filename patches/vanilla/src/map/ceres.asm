include

;;; tiles for graph area Ceres

;;; tiles for room "[Ceres Elevator Room]"

org map_data_Ceres+$398  ; (12, 14)
     dw $0c6e  ; BGtile[i=$6e p=$3 X]

org map_data_Ceres+$3d8  ; (12, 15)
     dw $0c1a  ; BGtile[i=$1a p=$3 X]

org map_data_Ceres+$418  ; (12, 16)
     dw $cc90  ; BGtile[i=$90 p=$3 HVX]

;;; tiles for room "[Ceres Jump Tutorial Room]"

org map_data_Ceres+$41a  ; (13, 16)
     dw $0c1d  ; BGtile[i=$1d p=$3 X]

org map_data_Ceres+$41c  ; (14, 16)
     dw $4c1d  ; BGtile[i=$1d p=$3 HX]

;;; tiles for room "[Ceres Staircase Room]"

org map_data_Ceres+$41e  ; (15, 16)
     dw $0c90  ; BGtile[i=$90 p=$3 X]

org map_data_Ceres+$45e  ; (15, 17)
     dw $cc90  ; BGtile[i=$90 p=$3 HVX]

;;; tiles for room "[Ceres Dead Scientists Room]"

org map_data_Ceres+$460  ; (16, 17)
     dw $0c1d  ; BGtile[i=$1d p=$3 X]

org map_data_Ceres+$462  ; (17, 17)
     dw $4c1d  ; BGtile[i=$1d p=$3 HX]

;;; tiles for room "[Ceres Last Corridor]"

org map_data_Ceres+$464  ; (18, 17)
     dw $0c1d  ; BGtile[i=$1d p=$3 X]

org map_data_Ceres+$466  ; (19, 17)
     dw $4c1d  ; BGtile[i=$1d p=$3 HX]

;;; tiles for room "[Ceres Ridley Room]"

org map_data_Ceres+$468  ; (20, 17)
     dw $4c05  ; BGtile[i=$05 p=$3 HX]
