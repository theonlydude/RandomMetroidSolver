include

;;; tiles for graph area Ceres

;;; tiles for room "[Ceres Elevator Room]"

org map_data_Ceres+$3b8  ; (28, 14)
     dw $4c6e  ; BGtile[i=$6e p=$3 HX]

org map_data_Ceres+$3f8  ; (28, 15)
     dw $4c1a  ; BGtile[i=$1a p=$3 HX]

org map_data_Ceres+$438  ; (28, 16)
     dw $8c90  ; BGtile[i=$90 p=$3 VX]

;;; tiles for room "[Ceres Jump Tutorial Room]"

org map_data_Ceres+$436  ; (27, 16)
     dw $4c1d  ; BGtile[i=$1d p=$3 HX]

org map_data_Ceres+$434  ; (26, 16)
     dw $0c1d  ; BGtile[i=$1d p=$3 X]

;;; tiles for room "[Ceres Staircase Room]"

org map_data_Ceres+$432  ; (25, 16)
     dw $4c90  ; BGtile[i=$90 p=$3 HX]

org map_data_Ceres+$472  ; (25, 17)
     dw $8c90  ; BGtile[i=$90 p=$3 VX]

;;; tiles for room "[Ceres Dead Scientists Room]"

org map_data_Ceres+$470  ; (24, 17)
     dw $4c1d  ; BGtile[i=$1d p=$3 HX]

org map_data_Ceres+$46e  ; (23, 17)
     dw $0c1d  ; BGtile[i=$1d p=$3 X]

;;; tiles for room "[Ceres Last Corridor]"

org map_data_Ceres+$46c  ; (22, 17)
     dw $4c1d  ; BGtile[i=$1d p=$3 HX]

org map_data_Ceres+$46a  ; (21, 17)
     dw $0c1d  ; BGtile[i=$1d p=$3 X]

;;; tiles for room "[Ceres Ridley Room]"

org map_data_Ceres+$468  ; (20, 17)
     dw $0c05  ; BGtile[i=$05 p=$3 X]
